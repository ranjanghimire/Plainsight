/**
 * Width reserved from the viewport’s right edge for opening the menu drawer
 * (see `useDrawerGestures`). Category horizontal swipes must ignore touches
 * that start here so that behavior stays unchanged.
 */
export const MENU_RIGHT_EDGE_SWIPE_PX = 20;
